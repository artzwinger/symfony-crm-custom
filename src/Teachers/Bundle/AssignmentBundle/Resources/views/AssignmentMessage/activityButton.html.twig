{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.clientButton({
    'dataUrl': path('teachers_assignment_message_create', oro_action_params(entity, 'activity')),
    'aCss': 'no-hash',
    'iCss': 'fa-envelope',
    'dataId': entity.id,
    'label': 'teachers.assignment.message.add_entity'|trans,
    'title': 'teachers.assignment.message.add_entity.title'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : true,
        'reload-grid-name': 'user-assignment-private-notes-grid',
        'refresh-widget-alias': 'activity-list-widget',
        'options' : {
            'alias': 'assignment-private-note-dialog',
            'dialogOptions' : {
                'title' : 'teachers.assignment.message.add_entity'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 1000
            }
        }
    }
}) }}
