{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.clientButton({
    'dataUrl': path('teachers_refund_create', oro_action_params(entity, 'activity')),
    'aCss': 'no-hash',
    'iCss': 'fa-undo',
    'dataId': entity.id,
    'label': 'teachers.refund.add_entity'|trans,
    'title': 'teachers.refund.add_entity.title'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : true,
        'reload-grid-name': 'user-refunds-grid',
        'refresh-widget-alias': 'activity-list-widget',
        'options' : {
            'alias': 'refund-dialog',
            'dialogOptions' : {
                'title' : 'teachers.refund.add_entity'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 1000
            }
        }
    }
}) }}
