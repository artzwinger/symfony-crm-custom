{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.clientLink({
    'dataUrl': path('teachers_payment_create', oro_action_params(entity, 'activity')),
    'aCss': 'dropdown-item no-hash',
    'iCss': 'fa-briefcase',
    'dataId': entity.id,
    'label': 'teachers.payment.add_entity'|trans,
    'title': 'teachers.payment.add_entity.title'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : true,
        'reload-grid-name': 'user-payments-grid',
        'refresh-widget-alias': 'activity-list-widget',
        'options' : {
            'alias': 'payment-dialog',
            'dialogOptions' : {
                'title' : 'teachers.payment.add_entity'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 1000
            }
        }
    }
}) }}
