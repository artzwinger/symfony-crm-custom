{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set widgetContent %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('teachers_bid_user_bids', { user: entity.id }),
        'title': 'teachers.bid.user_bids'|trans
    }) }}
{% endset %}
{{ UI.scrollSubblock(null, [widgetContent]) }}
