{% if value %}
    {% import 'OroUIBundle::macros.html.twig' as UI %}

    {% set bidId = record.getValue('id') %}
    {{ UI.clientLink({
        'dataUrl': path('teachers_bid_widget_info', {id : bidId}),
        'aCss': 'no-hash',
        'label' : value,
        'widget' : {
            'type' : 'dialog',
            'multiple' : false,
            'options' : {
                'alias': 'bid_info_widget_' ~ bidId,
                'dialogOptions' : {
                    'title' : value,
                    'allowMaximize': true,
                    'allowMinimize': true,
                    'dblclick': 'maximize',
                    'maximizedHeightDecreaseBy': 'minimize-bar',
                    'width': 600
                }
            }
        },
    }) }}
{% endif %}
