{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set widgetContent %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('teachers_satisfaction_user_satisfactions', { user: entity.id }),
        'title': 'teachers.satisfaction.user_satisfactions'|trans
    }) }}
{% endset %}
{{ UI.scrollSubblock(null, [widgetContent]) }}
