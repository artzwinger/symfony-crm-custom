{% extends 'OroUIBundle:actions:view.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as ui %}
{% set subject = entity|oro_format_name|default('N/A'|trans) %}
{% oro_title_set({ params : {"%application.subject%": subject} }) %}

{% block pageHeader %}
    {% set breadcrumbs = {
        'entity': entity,
        'indexPath': path('teachers_application_index'),
        'indexLabel': 'teachers.application.entity_plural_label'|trans,
        'entityTitle': subject
    } %}
    {{ parent() }}
{% endblock pageHeader %}

{% block navButtons %}
    {% import 'OroUIBundle::macros.html.twig' as UI %}

    {% if is_granted('teachers_application_delete') %}
        {{ UI.deleteButton({
            'dataUrl': path('teachers_application_delete', {'id': entity.id}),
            'dataRedirect': path('teachers_application_index'),
            'aCss': 'no-hash remove-button',
            'id': 'btn-remove-application-form',
            'dataId': entity.id,
            'entity_label': 'teachers.application.entity_label'|trans,
        }) }}
        {{ UI.buttonSeparator() }}
    {% endif %}

    {% if is_granted('teachers_application_edit') %}
        {{ UI.editButton({
            'path': path('teachers_application_update', {'id': entity.id}),
            'entity_label': 'teachers.application.entity_label'|trans,
        }) }}
    {% endif %}
{% endblock %}

{% block content_data %}
    {% set applicationInformationWidget %}
        {{ oro_widget_render({
            'widgetType': 'block',
            'url': path('teachers_application_info', {id: entity.id}),
            'title': 'teachers.application.block.application_information'|trans
        }) }}
    {% endset %}

    {% set dataBlocks = [
        {
            'title': 'General'|trans,
            'subblocks': [
                {'data': [applicationInformationWidget]}
            ]
        }
    ] %}

    {% set id = 'applicationView' %}
    {% set data = {'dataBlocks': dataBlocks} %}
    {{ parent() }}
{% endblock %}
